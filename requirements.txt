# requirements.txt
# Python dependencies for Flight Planner deployment

# Core dependencies
streamlit>=1.28.0
openai>=1.0.0
requests>=2.31.0

# No additional packages needed - all other modules are built-in Python
# (sqlite3, hashlib, json, math, datetime are all standard library)
